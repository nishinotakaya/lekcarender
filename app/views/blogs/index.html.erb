<p id="notice"><%= notice %></p>

<%= link_to 'カレンダー編集', all_blogs_edit_blog_path(@blogs) %>
<%= link_to 'カレンダー編集個別', new_blog_path %>
<h1>レクカレンダー</h1>>

<br>

<%= link_to '日記をかく', new_blog_path %>

<%= month_calendar events: @blogs do |date, blogs| %>
  <%= date.day %>

  <% blogs.each do |blog| %>
    <div>
      <%= link_to blog.title, blog %>
    </div>
  <% end %>
<% end %>
