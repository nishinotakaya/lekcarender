<div class="user_login">
  <h2><%= t('user.sessions.new.sign_in') %></h2>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "user" }) do |f| %>

    <div class="field">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
    </div> 

    <div class="field">
      <%= f.label :パスワード %><br />
      <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
    </div>

    <div class="field">
      <%= f.submit t('user.sessions.new.sign_in'), class: "btn btn-primary" %>
      <br><%= link_to "新規登録", new_user_registration_path %>
       <br><%= link_to "Lineログイン", user_line_omniauth_authorize_path %>
    </div>
  <% end %>
</div>
