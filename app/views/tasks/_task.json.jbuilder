json.extract! task, :id, :classification, :total, :manager, :ivc, :title, :st, :edi, :firstshipping, :firststock, :finishwarehouse, :integrationinstance, :hubinstance, :user_id, :created_at, :updated_at
json.url task_url(task, format: :json)
